# Dependencies
markdown:       kramdown
highlighter: 	rouge
gems: 		[jekyll-sitemap, jekyll-paginate]

# Permalinks
permalink:        pretty

# Setup
title:            'slightly hacked lanyon theme'
tagline:          'wow, so value, so bark'
description:      'i dont know'
url:              https://someurl.io

author:
  name:           'author'
  url:            https://url.io

paginate:         7

# Custom vars

#github:
#  repo:           https://github.com/some/repo

prose:
  rooturl: '_posts'
  siteurl: 'https://someurl.io'
  media: 'media'
  ignore:
    - index.md
    - _config.yml
    - /_layouts
    - /_includes
  metadata:
    _posts:
      - name: "title"
        field:
          element: "text"
          label: "title"
      - name: "date"
        field:
          element: "hidden"
          value: "CURRENT_DATETIME"
      - name: "layout"
        field:
          element: "hidden"
          value: "post"
      - name: "published"
        field:
          element: "hidden"
          value: "true"

